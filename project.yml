# XcodeGen descriptor — run `xcodegen` to regenerate Zettair.xcodeproj.
# https://github.com/yonaskolb/XcodeGen
name: Zettair
options:
  bundleIdPrefix: io.zettair
  deploymentTarget:
    iOS: "16.0"
  developmentLanguage: en
  createIntermediateGroups: true
settings:
  base:
    SWIFT_VERSION: "5.9"
    IPHONEOS_DEPLOYMENT_TARGET: "16.0"
    TARGETED_DEVICE_FAMILY: "1,2"     # iPhone + iPad
    ENABLE_USER_SCRIPT_SANDBOXING: NO
    SWIFT_STRICT_CONCURRENCY: minimal
    GENERATE_INFOPLIST_FILE: YES
    MARKETING_VERSION: "0.1.0"
    CURRENT_PROJECT_VERSION: "1"
    INFOPLIST_KEY_CFBundleShortVersionString: "0.1.0"
    INFOPLIST_KEY_CFBundleVersion: "1"

packages:
  ZettairKit:
    path: ZettairKit

targets:

  # ---------------- Main app ----------------
  ZettairApp:
    type: application
    platform: iOS
    sources:
      - path: ZettairApp
    resources:
      - path: ZettairApp/Resources
        optional: true
    dependencies:
      - package: ZettairKit
      # Widget + share extension are dropped from the side-load build because
      # free Apple ID accounts can't register App Groups in the developer
      # portal. Re-add when on a paid Developer Program.
    info:
      path: ZettairApp/Info.plist
      properties:
        CFBundleDisplayName: Zettair
        CFBundleShortVersionString: "0.1.0"
        CFBundleVersion: "1"
        UILaunchScreen:
          UIColorName: ""
        UIApplicationSceneManifest:
          UIApplicationSupportsMultipleScenes: false
        ITSAppUsesNonExemptEncryption: false
        NSAppTransportSecurity:
          NSAllowsArbitraryLoads: false
        CFBundleURLTypes:
          - CFBundleURLName: io.zettair.app
            CFBundleURLSchemes:
              - zettair
        UIBackgroundModes: []
        NSUserActivityTypes:
          - io.zettair.app.search
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: io.zettair.app
        INFOPLIST_KEY_UILaunchScreen_Generation: YES
        INFOPLIST_KEY_UISupportedInterfaceOrientations_iPhone: "UIInterfaceOrientationPortrait UIInterfaceOrientationLandscapeLeft UIInterfaceOrientationLandscapeRight"
        INFOPLIST_KEY_UISupportedInterfaceOrientations_iPad: "UIInterfaceOrientationPortrait UIInterfaceOrientationPortraitUpsideDown UIInterfaceOrientationLandscapeLeft UIInterfaceOrientationLandscapeRight"
        CODE_SIGN_ENTITLEMENTS: ZettairApp/ZettairApp.entitlements
        CODE_SIGN_STYLE: Automatic
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon

schemes:
  ZettairApp:
    build:
      targets:
        ZettairApp: all
    run:
      config: Debug
    test:
      config: Debug
    archive:
      config: Release
